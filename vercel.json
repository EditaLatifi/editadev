{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "vite build && (vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender-ssr.mjs || true)",
  "outputDirectory": "dist"
}
